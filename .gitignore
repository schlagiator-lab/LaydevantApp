# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dev-dist
.wrangler
*.local

# Cache local du CLI Supabase (project ref/org id régénérés par `supabase
# link`) — machine-spécifique, jamais versionné
supabase/.temp/

# Env files (never commit secrets, even the anon key — keep it out of history)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
